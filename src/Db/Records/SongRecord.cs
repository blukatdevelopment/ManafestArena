public struct SongRecord {
  string title, file, album, author;

  public SongRecord(string title, string file, string album, string author){
    this.title = title;
    this.file = file;
    this.album = album;
    this.author = author;
  }
}